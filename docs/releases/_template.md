# Release Notes — vX.Y.Z

**Release Date**: YYYY-MM-DD
**Status**: <one-line summary>

## Summary

<2–4 paragraphs on what changed and why>

## What's New

<grouped by brief / feature area>

## Upgrade Notes

<any operator-visible changes; if none, say so explicitly>

## Verification

<!--
AC #3a (PER-030, entarch 2026-05-11 P2 + secrev concurrence):

Each docs/releases/vX.Y.Z.md MUST do ONE of the following two things,
chosen per release by the implementer:

(a) Inline the stable key fingerprints + verification commands
    directly. Denser; useful when the release notes are read
    standalone (e.g., on a download page).

(b) Hard-pointer to /RELEASE_CHECKLIST.md as the canonical
    verification source. Single source of truth; useful when
    fingerprints have been stable for multiple releases.

A release notes file with NO fingerprints AND NO checklist pointer
fails AC #3a. Pick (a) or (b); do not ship neither.

The hard-pointer option below is the default; replace with the
inline option if you prefer per release.
-->

For binary verification commands, stable key fingerprints, and the
canonical procedure for verifying a downloaded chanvoy release, see
[`/RELEASE_CHECKLIST.md`](../../RELEASE_CHECKLIST.md) (specifically §11
"Verification commands for external adopters").

## Briefs in this release

<bulleted list of PER-XXX / other brief codes shipped in this release>
